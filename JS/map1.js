ymaps.ready(init);
var myMap;
function init(){     
const map = new ymaps.Map('map', {
    center: [54.989001, 82.905149],
    zoom: 13,
    // выключение элементов управления.
    controls: []
  });
}